apiVersion: v1
kind: PersistentVolume
metadata:
  name: apps-postgres
  namespace: default
  labels:
    type: local
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 10Gi
  hostPath:
    path: "/var/mnt/apps/postgres"

