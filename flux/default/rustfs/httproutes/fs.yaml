apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fs
  namespace: default
spec:
  parentRefs:
  - name: private
    namespace: kube-system
    sectionName: fs
  hostnames:
  - fs.${DOMAIN}
  rules:
  - backendRefs:
    - name: rustfs
      port: 9001

