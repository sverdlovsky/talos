apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mail
  namespace: default
spec:
  secretName: mail-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - mail.${DOMAIN}

