apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: top
  namespace: kube-system
spec:
  secretName: top-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - top.${DOMAIN}

