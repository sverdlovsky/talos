apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: top
  namespace: kube-system
spec:
  parentRefs:
  - name: private
    namespace: kube-system
    sectionName: top
  hostnames:
  - top.${DOMAIN}
  rules:
  - backendRefs:
    - name: hubble-ui
      port: 80

