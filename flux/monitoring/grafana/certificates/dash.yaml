apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dash
  namespace: kube-system
spec:
  secretName: dash-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - dash.${DOMAIN}

