apiVersion: v1
kind: Service
metadata:
  name: tempo
  namespace: monitoring
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  selector:
    app: tempo
  ports:
  - name: tempo
    port: 3200
    targetPort: 3200
    protocol: TCP

