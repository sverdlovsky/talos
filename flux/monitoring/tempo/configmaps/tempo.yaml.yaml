apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo
  namespace: monitoring
data:
  tempo.yaml: |
    storage:
      trace:
        backend: local
        local:
          path: /var/tempo

        #backend: s3
        #s3:
        #  endpoint: ${S3_ENDPOINT}
        #  bucket: ${S3_BUCKET}
        #  access_key: ${S3_ACCESS_KEY}
        #  secret_key: ${S3_SECRET_KEY}
        #  insecure: true

