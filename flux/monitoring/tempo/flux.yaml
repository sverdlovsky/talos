apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tempo
  namespace: flux-system
spec:
  dependsOn:
  - name: cilium
  - name: tetragon
  - name: rustfs
  interval: 15m0s
  path: ./flux/monitoring/tempo
  prune: true
  wait: true
  timeout: 5m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  postBuild:
    substituteFrom:
    - kind: Secret
      name: flux-cluster
    - kind: Secret
      name: flux-tempo

