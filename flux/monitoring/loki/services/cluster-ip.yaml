apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: monitoring
spec:
  type: ClusterIP
  ipFamilyPolicy: PreferDualStack
  selector:
    app: loki
  ports:
  - name: loki
    port: 3100
    targetPort: 3100
    protocol: TCP

