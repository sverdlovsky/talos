apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: loki
  namespace: kube-system
spec:
  secretName: loki-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - loki.${DOMAIN}

