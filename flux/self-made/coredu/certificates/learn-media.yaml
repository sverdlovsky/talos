apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: learn-media
  namespace: kube-system
spec:
  secretName: learn-media-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - media.learn.${DOMAIN}

