apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: learn-api
  namespace: kube-system
spec:
  secretName: learn-api-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - api.learn.${DOMAIN}

