apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: learn-auth
  namespace: kube-system
spec:
  secretName: learn-auth-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - auth.learn.${DOMAIN}

