apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: learn
  namespace: kube-system
spec:
  secretName: learn-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - learn.${DOMAIN}

