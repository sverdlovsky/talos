apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: auth
  namespace: self-made
spec:
  parentRefs:
  - name: public
    namespace: kube-system
    sectionName: auth
  hostnames:
  - auth.${DOMAIN}
  rules:
  - backendRefs:
    - name: yaoae
      port: 8080

