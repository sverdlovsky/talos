apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: video-api
  namespace: self-made
spec:
  parentRefs:
  - name: public
    namespace: kube-system
    sectionName: video-api
  hostnames:
  - api.video.${DOMAIN}
  rules:
  - backendRefs:
    - name: svh
      port: 8080

