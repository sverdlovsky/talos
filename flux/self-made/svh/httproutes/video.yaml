apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: video
  namespace: self-made
spec:
  parentRefs:
  - name: public
    namespace: kube-system
    sectionName: video
  hostnames:
  - video.${DOMAIN}
  rules:
  - backendRefs:
    - name: svh
      port: 80

