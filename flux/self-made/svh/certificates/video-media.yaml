apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: video-media
  namespace: kube-system
spec:
  secretName: video-media-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - media.video.${DOMAIN}

