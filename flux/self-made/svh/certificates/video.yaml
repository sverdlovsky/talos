apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: video
  namespace: kube-system
spec:
  secretName: video-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - video.${DOMAIN}

