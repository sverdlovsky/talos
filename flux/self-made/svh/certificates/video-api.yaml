apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: video-api
  namespace: kube-system
spec:
  secretName: video-api-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  dnsNames:
  - api.video.${DOMAIN}

